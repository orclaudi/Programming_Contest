#include <stdio.h>

int main(void)
{
    int x;
    int n;
    int *a;

    scanf("%d", &n);
    scanf("%d", &x);
    for(int i = 0; i < n; i++)
    {
        scanf("%d", a[i])
    }
    for(int i = 0; i < n; i++)
    {
        if (a[i] + a[i + 1])
        {
            print
        }
    }
}